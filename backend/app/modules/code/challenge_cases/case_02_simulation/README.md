# Case 02: fixed-seed Monte Carlo parameter comparison

Compares Monte Carlo estimates of pi at three sample budgets with seed 42.
The emitted metrics are measured by the script, not stored mock values.

Run from `backend/`:

```bash
python -m app.modules.code.challenge_cases.case_02_simulation.run --output data/challenge_cup/case_02
```
